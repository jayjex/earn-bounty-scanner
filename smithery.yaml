# Smithery configuration — https://smithery.ai
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({ "command": "npx", "args": ["-y", "earn-bounty-scanner"] })
